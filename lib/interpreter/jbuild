(jbuild_version 1)

(library
 ((name Interpreter)
  (public_name datamint.interpreter)
  (preprocess (pps (ppx_deriving.show)))
  (libraries (datamint.series datamint.kyoto yojson lambda-term))))

(ocamllex (lexer))
(ocamlyacc (parser))
